This is where all files built from `src` by `bin/index` during the `before_build` hook (see `config.xml`) will be created. If no `www` folder is present cordova will complain this is not a cordova project.